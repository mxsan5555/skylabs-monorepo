{
  "name": "mera-driver-api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/mera-driver-api/src",
  "projectType": "application",
  "targets": {
    "prisma-generate": {
      "cache": true,
      "inputs": ["{projectRoot}/prisma/schema.prisma"],
      "outputs": ["{projectRoot}/src/generated"],
      "executor": "nx:run-commands",
      "options": {
        "command": "prisma generate --schema=apps/mera-driver-api/prisma/schema.prisma"
      }
    },
    "build": {
      "dependsOn": ["prisma-generate", "^build"]
    },
    "prune-lockfile": {
      "dependsOn": ["build"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/dist/apps/mera-driver-api/package.json",
        "{workspaceRoot}/dist/apps/mera-driver-api/package-lock.json"
      ],
      "options": {
        "buildTarget": "build"
      }
    },
    "copy-workspace-modules": {
      "dependsOn": ["build"],
      "cache": true,
      "outputs": [
        "{workspaceRoot}/dist/apps/mera-driver-api/workspace_modules"
      ],
      "executor": "@nx/js:copy-workspace-modules",
      "options": {
        "buildTarget": "build"
      }
    },
    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "mera-driver-api:build",
        "runBuildTargetDependencies": false
      }
    }
  },
  "tags": []
}
